{
  "name": "seoforge-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/ || true",
    "start": "node dist/worker.js"
  },
  "dependencies": {
    "bullmq": "^1.50.0",
    "ioredis": "^5.3.2"
  }
}
