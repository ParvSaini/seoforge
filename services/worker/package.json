{
  "name": "seoforge-worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/worker.js",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/",
    "start": "node dist/worker.js",
    "dev": "node src/worker.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=14"
  }
}
