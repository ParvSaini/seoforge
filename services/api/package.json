{
  "name": "seoforge-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/ || true",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bullmq": "^1.50.0",
    "ioredis": "^5.3.2"
  }
}
