{
  "name": "seoforge-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist; mkdir -p dist; cp -R src/* dist/ || true",
    "start": "node dist/index.js",
    "start:dev": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bullmq": "^1.50.0",
    "ioredis": "^5.3.2"
  }
}
