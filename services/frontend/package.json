{
  "name": "seoforge-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist; mkdir -p dist; cp -R public/* dist/ || true",
    "start": "node server.js"
  },
  "dependencies": {}
}
