{
  "name": "seoforge-frontend",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p dist && cp -R public/* dist/",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14"
  }
}
